const buttons=[...document.querySelectorAll('#nav button')];const pages=[...document.querySelectorAll('.page')];const title=document.getElementById('pageTitle');buttons.forEach(btn=>btn.addEventListener('click',()=>{const id=btn.dataset.page;buttons.forEach(x=>x.classList.toggle('active',x===btn));pages.forEach(p=>p.classList.toggle('active',p.id===id));title.textContent=btn.firstChild.textContent.trim();window.scrollTo({top:0,behavior:'smooth'});}));